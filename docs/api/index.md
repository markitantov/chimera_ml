# API reference

The reference below is generated from the installed `chimera_ml` package with
`mkdocstrings`. It is intentionally small at this stage and starts with the
public containers and registry abstraction used throughout the framework.

## Core data containers

::: chimera_ml.core.batch.Batch

::: chimera_ml.core.types.ModelOutput

## Registries

::: chimera_ml.core.registry.Registry
